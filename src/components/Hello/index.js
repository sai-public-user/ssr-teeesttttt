import React from 'react';
import { NavLink } from 'react-router-dom';

export default ({ name, flag, capital, population })  => {
  return (
    <h1 style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100%', color: 'linear-gradient(to bottom, #33ccff 0%, #ff99cc 100%)' }}>Hello World</h1>
  );
};